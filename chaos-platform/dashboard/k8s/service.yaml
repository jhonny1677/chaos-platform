apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: chaos-engine
  labels:
    app: dashboard
spec:
  selector:
    app: dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
